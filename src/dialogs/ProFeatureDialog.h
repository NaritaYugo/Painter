#pragma once

class QWidget;
class QString;

// ---------------------------------------------------------------------------
// ProFeatureDialog
// ---------------------------------------------------------------------------
// Pro版限定の機能を、無料版ビルドで(またはPro版ビルドでもライセンス未認証の状態で)
// 呼び出そうとしたときに出す案内ダイアログ。どのPro限定機能からも共通で使い回す
// ための唯一の窓口として、この1関数だけを公開する。
//
// 無料版ビルド/Pro版ビルドどちらでも常にコンパイルされる(呼び出し側の
// #ifdef TIEPOLO_PRO_BUILD の外側=無料版の分岐で使うため)。
// ---------------------------------------------------------------------------
namespace ProFeatureDialog {
    // featureName: 「色収差」のような機能名(ダイアログの本文に埋め込む)。
    void show(QWidget *parent, const QString &featureName);
}
